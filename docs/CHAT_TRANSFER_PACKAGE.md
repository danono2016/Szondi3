# SZONDI3 — CHAT TRANSFER PACKAGE

**Purpose:** recover the project safely without depending on prior chat history.  
**Repository:** `danono2016/Szondi3`  
**Authoritative state:** current repository state plus independently verified CI  
**Accepted gates:** `P0_SOURCES_PASS`; `P1_DETERMINISTIC_ENGINE_PASS`  
**Current development phase:** `P2A — Primary Doctrine Registry` — ACTIVE, corpus gate not yet declared  
**Next phase after P2A:** `P2B — Executable Interpretation`

> **CURRENT OVERRIDES — 2026-08-27:** post-P1 work has resolved two formerly open items. `kp/hs` is RESOLVED under Decision Log `D-014` / `docs/KP_HS_RESOLUTION.md`; the extended abbreviated representation is the strongly source-constrained symptomatic-line/root-line projection with the median line omitted. The Lehrbuch `U003912` Triventil OCR blocker is RESOLVED under `D-015` / `docs/TRIVENTIL_VISUAL_ARBITRATION.md` as the interval `3–4`. These later decisions supersede older residual-language in historical P1 records where explicitly marked.

## Successor start — mandatory READ ONLY

A successor chat receives no write authority merely from this package.

First verify the current repository/branch/PR/CI state independently. Then read and follow `docs/CHAT_SUCCESSION_PROTOCOL.md` exactly. The required normative/state reading order includes:

1. `docs/PROJECT_CONSTITUTION.md`
2. `docs/DOCTRINAL_FIDELITY_POLICY.md`
3. `docs/FOUNDATION_ARCHITECTURE.md`
4. `docs/DEVELOPMENT_GOVERNANCE.md`
5. `docs/VALIDATION_AND_RECOVERY.md`
6. `docs/PROJECT_CHECKPOINT.md`
7. `docs/RESTART_ROADMAP.md`
8. `docs/P1_DETERMINISTIC_ENGINE_VERIFICATION.md`
9. `docs/P1_RESOLUTION_SWEEP.md`
10. `docs/CANONICAL_ACCESS_SPEC.md`
11. `docs/STIMULUS_MAPPING_MANIFEST.md`
12. `config/source_catalog.json`
13. `config/evidence_lock.json`
14. `docs/LEHRBUCH_P2A_PROGRESS.md`
15. `docs/P2A_CORPUS_INTEGRATION_ARCHITECTURE.md`
16. `docs/KP_HS_RESOLUTION.md`
17. `docs/TRIVENTIL_VISUAL_ARBITRATION.md`
18. `docs/ENGINE_PROVENANCE_GAP_AUDIT.md`
19. `docs/P2B_EXECUTABLE_INTERPRETATION_SPEC.md`

For formal takeover, complete the qualification rubric/template and respect the current scoped authorization and source ownership. Recheck active PRs before writing because source-local P2A work can advance in parallel.

## Core project rule

Szondi3 is a clean reconstruction from admitted sources and its own specifications.

`PRIMARY EVIDENCE -> DOCTRINE -> EXECUTABLE INTERPRETATION -> SOFTWARE BEHAVIOR`

A downstream layer may not rewrite or silently modernize an upstream layer. Legacy projects are outside the Szondi3 development and validation path and are not source truth, design authority, implementation templates or gates.

For source work, the approved canonical evidence is the default textual memory; paired admitted PDFs are visual arbiters only where tables, formulas, symbols, OCR loss or spatial layout materially affect meaning.

## Evidence boundary

The admitted repository evidence remains:

- 10 DOCX source files;
- 8 PDF visual arbiters;
- 48 WebP stimuli.

Identity is machine-locked by `config/evidence_lock.json`.

Source layers remain separate:

- eight `SZ_*` entries = `SZONDI_PRIMARY`;
- `DERI_1949` and `MELON_1975` = post-Szondian tradition.

Do not modernize or sanitize Szondi-primary wording. Genetic/hereditary/genotropic/transgenerational formulations, sexual/pathological vocabulary and historically anachronistic terminology remain part of the primary layer. Client-facing softening belongs only downstream.

Historical metadata about photographed persons is excluded from runtime scoring, doctrine, interpretation and reports.

## Accepted gates

### P0

`P0_SOURCES_PASS` remains accepted. The corpus/stimuli are identity-verifiable, canonical access is deterministic and the 48-card mapping is directly primary-source verified.

### P1

`P1_DETERMINISTIC_ENGINE_PASS` remains accepted.

The deterministic engine covers stimulus identity/presentation, foreground administration, VGP, EKP/background complements, factor scoring, vectors/profiles, repeated series, Tabelle 13 normalization, formal indices/classifications, Triebklasse/Unterklasse source-safe core, complete/abbreviated Triebformel core, Dur-Moll arithmetic and Sozialindex arithmetic. Post-P1 D-014 adds the resolved extended abbreviated representation; D-015 confirms the Triventil `3–4` interval by approved-source visual arbitration.

PR #37 is the semantic closure witness, merged as `5e4d02782d7165dbcea7828ca055a2415b72d262`. PR #38 is the durable gate-finalization witness, merged as `0cfe097f10c445044fcc60f561d60aae3e299dd2`.

See `docs/P1_DETERMINISTIC_ENGINE_VERIFICATION.md`, `docs/P1_RESOLUTION_SWEEP.md`, `docs/KP_HS_RESOLUTION.md` and `docs/TRIVENTIL_VISUAL_ARBITRATION.md` for evidence and boundaries.

## Current P2A state

### Lehrbuch — `SZ_LEHR_1972`

- source-order read: complete to EOF;
- canonical BODY coverage: complete `U000584–U008416 / EOF`;
- retained standalone doctrine: 166;
- open-ended Lehrbuch extraction is closed;
- no active Lehrbuch numeric blocker remains.

Authoritative working checkpoint: `docs/LEHRBUCH_P2A_PROGRESS.md`.

### Ich-Analyse — PR #52, parallel source-local writer

- `SZ_IA_1956_A`: source-local P2A coverage complete to EOF;
- `SZ_IA_1956_B`: book-level read complete and source-order P2A active;
- latest checked continuation checkpoint on 2026-08-27 recorded B coverage through `BODY U000181`, next `U000182`; this is a volatile snapshot and **must be rechecked** before integration;
- this Lehrbuch branch must not edit IA source-local registry/coverage artifacts.

No corpus-wide `P2A_PRIMARY_DOCTRINE_PASS` is declared yet.

## Important residual boundaries

These must not be mistaken for unfinished deterministic implementation:

- `kp/hs` is **not** an active research blocker. It is resolved by D-014. Only ambiguity in the underlying complete-formula partition remains local and fail-closed; simple-form extrema ties remain separate.
- tied simple abbreviated extrema remain candidates unless a source-authorized selector resolves them;
- mixed Wurzelfaktor direction has no invented majority Unterklasse sign;
- non-unique complete-Triebformel partitions fail closed;
- Quantenverrechnung remains source-described as incomplete;
- exact Böszörményi Inkonstanzmethode calculation is not reconstructed without the identified original publication;
- clinical/qualitative short-series constancy, Rand-Mitte, verbal/association methods and clinical meanings of Dur-Moll/Sozialindex route downstream.

New evidence may reopen a boundary through governance. A successor must not treat reopening as permission to retrofit an unsupported rule.

## Next safe work

The immediate project path is:

1. keep Lehrbuch source-order P2A closed and regression-protect P1;
2. allow the IA writer to continue `SZ_IA_1956_B` source-locally;
3. integrate compact source-local doctrine only after referenced IDs are stable on `main`;
4. build only the small transversal concept/relation index required for retrieval;
5. review/refine `docs/P2B_EXECUTABLE_INTERPRETATION_SPEC.md` against the integrated doctrine set;
6. then implement P2B incrementally from high-confidence structural semantics, safeguards and explicitly representable polysemy.

The criterion is not database size. It is a test engine that can calculate and interpret faithfully with reconstructable provenance and explicit uncertainty.

## Recovery commands

At minimum:

```bash
python scripts/verify_foundation.py
python -m unittest discover -s tests -p 'test_*.py' -v
```

For full deterministic source-access verification, follow `.github/workflows/p0-canonical-access.yml`.

## Stop / repair rule

If new evidence conflicts with an accepted assumption, reopen the affected boundary and repair the lowest affected layer. Do not repair executable logic by rewriting source doctrine, and do not convert uncertainty into an unsupported default.
