# Lehrbuch P2A compaction audit — DR 000153–000178

**Source:** `SZ_LEHR_1972`

## Result

Original standalone objects: **26**  
Retained standalone cores: **7**  
Reduction: **19 objects / 73.1%**

## Retained cores

- `000154` — distinction between universal **Bausteine** (factors/vectors/64 Vektorenbilder) and the individual's **Plan**; later interpretation reconstructs the individual plan.
- `000157` — compact **factor h foundation**: Erosradikal, love/tenderness/attraction, hermaphroditic starting form, Libido as h-specific energy, and Szondi's non-convertible multi-energy/Ego-change framework.
- `000167` — general **relative-measurement rule**: test quantities express interfactorial relations, never absolute amounts of drive energy.
- `000165` — compact `0h` doctrine: relative current reduction/absence; recent discharge, social/sublimated satisfaction, rarely constitutional weakness; never absolute permanent absence.
- `000171` — compact `+h` doctrine: positive Eros/attachment, with Überdruck as Eroshypertonie affirmed by Ego.
- `000174` — compact `−h` doctrine: possible Menschheitsliebe only under appropriate `s` constellation; otherwise repression/suppression/renunciation of Personenliebe; strict Partnerfaktor correlation required.
- `000176` — compact `±h` doctrine: native hermaphroditic/undifferentiated Eros; with `±s`, bisexual undifferentiated stage; related to immature/undifferentiated Ego choice.

## Retirement map

- `000153`, `000155–000156` → `MERGE_INTO_CORE -> 000154` or `OMIT_NONCRITICAL` as pedagogical restatement.
- `000158–000164` → `MERGE_INTO_CORE -> 000157`; energy-source and Ego-transformation theory remain represented without six standalone entries.
- `000166`, `000169–000170` → `MERGE_INTO_CORE -> 000165`.
- `000168` → `MERGE_INTO_CORE -> 000167`.
- `000172` → `MERGE_INTO_CORE -> 000171`.
- `000173`, `000175` → `MERGE_INTO_CORE -> 000174`.
- `000177` → `MERGE_INTO_CORE -> 000176`.
- `000178` → `OMIT_NONCRITICAL` — chapter-scope disclaimer remains in canonical context.

## Fidelity safeguards

- Hermaphroditic/bisexual, homoerotic, repression and Humanisierung terminology is preserved at source force.
- `−h` is not simplified into Menschheitsliebe; the strict `s` Partnerfaktor dependency remains explicit.
- `0h` remains relative and multi-causal, not an absolute absence claim.
- The general relative-measurement rule remains separately retrievable because it constrains quantitative interpretation across all factors.
- Gentheorie/energy concepts are compacted, not silently discarded; their source-specific status remains attributable through anchors.
