# CH42 — Research

**Capitol:** 42 — `Triebklasse` și `Unterklasse`: `Wurzelfaktor`, `Triebgefahr` și `Ventil`  
**Statut:** RESEARCH COMPLETE — corpus pass 10/10 + bounded deep corpus pass + control vizual canonic al regulilor de clasă / OUTLINE COMPLETE / DRAFT NEXT  
**Ramură:** `manual`

---

## Întrebarea cognitivă

Capitolul 41 a stabilit mecanica:

`TspG -> TspD -> Latenzgrad / Latenzgröße -> Latenzproportionen`.

Capitolul 42 trebuie să răspundă la întrebarea imediat următoare:

**cum transformă Szondi ordinea celor patru grade de latență într-o `Triebklasse`, cum apare `Unterklasse`, ce este `Wurzelfaktor`-ul clasei și cum se separă formal `Triebgefahr` de `Triebventil`?**

Problema are două axe distincte, care trebuie predate separat:

1. **axa relativă:** care latență este cea mai mare și ce factor/direcție o poartă?  
   `Latenzproportionen -> Haupttriebklasse -> Unterklasse`;
2. **axa absolută:** cât de mare este fiecare `Latenzgröße` pe scara unei `Zehnerserie`?  
   `5–10 -> Triebgefahr`, `0–4 -> Triebventil`.

Această separare este nucleul pedagogic al capitolului. Dacă cele două axe sunt amestecate, cititorul riscă să creadă fie că „prima clasă” este automat o categorie clinică, fie că orice valoare maximă este automat o `Triebgefahr` în sensul pragului absolut.

Formula de lucru a research-ului este:

> **rangul relativ spune unde conduce latența; valoarea absolută spune dacă, pe scara canonică de zece, avem `Gefahr` sau `Ventil`.**

---

# I. Sursa primară decisivă — `Lehrbuch`, ediția 1972

Pentru cap. 42, sursa canonică principală este `Szondi Lehrbuch der experimentellen Triebdiagnostik`, în special pp. tipărite **278–284** și **288–290**, cu control vizual direct al pp. 280–284 și 288–289.

Această ediție este critică deoarece nu doar repetă mecanica veche a `Triebklasse`, ci **revizuiește explicit nomenclatura claselor**.

## 1. De la `Latenzproportionen` la `Haupttriebklasse`

Pe baza celor patru `TspD / Latenzgrade`, Szondi ordonează diferențele vectoriale după mărime.

Regula centrală este:

> vectorul cu **cea mai mare `Tendenzspannungsdifferenz` / cel mai mare `Latenzgrad`** indică locul celei mai mari latențe și al celei mai puternice `Triebgefahr`.

În exemplul canonic cu 15 profile:

`Sh / 13 : Schp / 4 : Cm / 4 : P / 0`

cea mai mare latență este `Sh / 13`; prin urmare, Szondi spune că persoana aparține actual **`Haupttriebklasse Sh`**.

La p. 280, formularea este condensată astfel:

1. cel mai mare `Latenzgrad` indică locul celei mai amenințătoare `Triebgefahr` — `Gefahrklasse`;
2. cel mai mic `Latenzgrad` indică cele mai accesibile `Notausgänge / Ventile`;
3. valorile intermediare își primesc rangul prin poziția relativă între maxim și minim.

Aceasta confirmă că **întreaga proporție rămâne relevantă**. Clasa conducătoare nu anulează celelalte trei valori.

## 2. Cele opt `Haupttriebklassen`

Cele patru vectori au câte doi factori posibili ca factor latent / index:

- S -> `Sh` sau `Ss`;
- P -> `Pe` sau `Phy`;
- Sch -> `Schk` sau `Schp`;
- C -> `Cd` sau `Cm`.

Rezultă **opt `Haupttriebklassen`**:

`Sh, Ss, Pe, Phy, Schk, Schp, Cd, Cm`.

Factorul care intră în numele clasei este factorul cu **`TspG` mai mic**, deja stabilit în cap. 41. El reprezintă, în vocabularul lui Szondi, trebuința mai latentă / mai puțin descărcată.

Aici `Wurzelfaktor` trebuie separat strict de simpla „valoare mică”:

- operațional, el este factorul cu `TspG` mai mic din vectorul relevant;
- doctrinar, Szondi îl tratează ca trebuință nesatisfăcută, latentă, dinamic determinantă și îl numește `Wurzelfaktor`;
- în alte volume îl leagă și de `Konduktornatur`; această teză genetică rămâne doctrină istorică, nu concluzie clinică modernă.

## 3. `Unterklasse`: semnul `Wurzelfaktor`-ului

La pp. 281–282, `Lehrbuch` împarte fiecare dintre cele opt clase principale în două `Unterklassen`.

Principiul este explicit:

- o latență poate avea direcție **pozitivă**;
- sau direcție **negativă**;
- semnul `+ / −` atașat clasei este semnul direcției factorului latent / `Wurzelfaktor`.

Rezultă **16 `Unterklassen`**:

`Sh+`, `Sh−`, `Ss+`, `Ss−`,  
`Pe+`, `Pe−`, `Phy+`, `Phy−`,  
`Schk+`, `Schk−`, `Schp+`, `Schp−`,  
`Cd+`, `Cd−`, `Cm+`, `Cm−`.

Semnul nu este:

- semnul unui `TspD` — `TspD` este o diferență de mărime;
- o evaluare morală pozitiv/negativ;
- semnul vectorului ca întreg.

Este **direcția `+ / −` a trebuinței latente / `Wurzelfaktor`-ului**.

### Justificarea doctrinară a semnului

Szondi oferă aici o explicație specifică teoriei sale:

- latența pozitivă poate rezulta dintr-un cuantum extrem de mare al unei trebuințe, care rămâne nesatisfăcută chiar dacă Eul îi afirmă satisfacerea; serial apar reacții pozitive persistente, mai ales cu `Überdruck`;
- latența negativă poate apărea prin `Verdrängung`, dar și prin `Anpassung` la mediu sau `Verzicht` sub presiunea Eului / Supra-Eului; acestea sunt făcute vizibile prin reacții negative.

Această justificare trebuie prezentată ca **doctrină Szondi în contextul construirii subclasei**. Nu trebuie transformată într-un dicționar universal `+ = ...`, `− = reprimare`.

## 4. `Hauptklasse` și `Unterklasse` sunt o axă relativă

Sursa spune foarte limpede:

> `Hauptklassen` au fost construite pe baza `Tendenzspannungsdifferenzen`;  
> `Unterklassen` pe baza pozitivității/negativității direcției trebuințelor nesatisfăcute, adică a `Wurzelfaktoren`.

Prin urmare:

`Latenzproportionen -> maxim relativ -> Haupttriebklasse -> semnul Wurzelfaktor-ului -> Unterklasse`.

Exemplu generic:

dacă proporția începe cu `Schp− / 10`, atunci:

- vector conducător: `Sch`;
- factor latent/indexat: `p`;
- direcție latentă: `−`;
- subclasa: **`Schp−`**.

Nu este nevoie de `Triebformel` pentru a numi subclasa. `Triebformel` va răspunde însă la o întrebare mai fină: **prin ce factori simptomatici / ventile se descarcă persoana și cum este configurată exact situația?** Aceasta rămâne cap. 43.

---

# II. A doua axă — `Triebgefahr` versus `Triebventil`

## 1. Pragul canonic este formulat pe `Zehnerserie`

La p. 282, `Lehrbuch` adaugă un al doilea criteriu, explicit **cantitativ și absolut**.

Textul spune:

> `Bei einer Zehnerserie` valorile `Latenzgröße` pot varia între 10 și 0.

Pe această scară:

- **`5, 6, 7, 8, 9, 10` -> wirkliche `Triebgefahren`;**
- **`4, 3, 2, 1, 0` -> `Triebventile`.**

Echivalent:

- `Latenzgröße > 4` -> `Gefahr / Wurzel`;
- `Latenzgröße < 5` -> `Ventil / Symptom`.

Aceasta este o regulă canonică directă și trebuie predată numeric, nu aproximativ.

## 2. `Ventil` nu înseamnă „adaptativ” sau „sănătos”

La p. 280, Szondi numește valorile mici `Notausgänge / Ventile`.

La p. 282, spune că în acest domeniu se caută `Triebventil`, „în sens clinic simptomul”.

În alte pasaje, ventilele pot fi paroxysmale, paranoide, manice etc.

Prin urmare:

> **`Ventil` este un canal de ieșire / descărcare în doctrina lui Szondi, nu un mecanism sănătos prin definiție.**

Nu trebuie tradus tacit prin `coping`, `resursă`, `reglare adaptativă` sau alt termen contemporan.

## 3. `Gefahr- / Wurzelklassen`

Dacă una sau mai multe valori din `Latenzproportionen` sunt **peste 4**, `Lehrbuch` grupează cazurile după numărul `Triebgefahren`:

- o `Gefahr`;
- două `Gefahren`;
- trei `Gefahren`;
- patru `Gefahren`.

Acest nivel nu trebuie confundat cu cele opt `Haupttriebklassen`.

Exemplu canonic de structură cu două pericole:

`Sh+ / 10 : Schp− / 9 : Cm / 2 : P / 1`

primele două valori sunt `Gefahren`, ultimele două `Ventile`.

Așadar, două informații coexistă:

- **care subclasă ocupă primul rang relativ**;
- **câte domenii trec pragul absolut de pericol**.

## 4. `Ventil- / Symptomklassen`

Dacă **toate patru** `Latenzgrößen` sunt sub 5, `Lehrbuch` vorbește despre `Ventil- oder Symptomklassen`.

În ediția 1972, pentru simplificarea `Linnäussystem`-ului:

- `Biventilklassen` nu mai sunt evidențiate ca grup separat;
- sunt stabilite două grupe:
  - `Triventilklasse`;
  - `Quadriventilklasse`.

### `Triventilklasse`

Când diferența dintre cea mai mare și cea mai mică `Latenzgröße` este **3–4**.

Sursa include aici și unele structuri „în sens relativ” biventile.

### `Quadriventilklasse`

Când toate patru valorile sunt sub 5 și diferența dintre maxim și minim este **sub 3**, adică `2`, `1` sau `0`.

Pentru cazurile tri- și quadriventile, Szondi spune că persoana este localizată în tabelele Linnäus pe baza **celei mai mari `Latenzgröße` relative**.

Această ultimă regulă trebuie păstrată împreună cu regula din cap. 41:

> **la egalitate nu inventăm tie-break.**

Pasajul controlat nu oferă o regulă universală suplimentară pentru cazul în care maximul relativ însuși este egal în două sau mai multe poziții.

---

# III. Revizia istorică a sistemului de clase — punct doctrinar major

Acesta este principalul rezultat al bounded deep pass-ului pentru cap. 42.

## 1. Stratul vechi: `inäqual / biäqual / triäqual / quadriäqual`

În sursele mai vechi și în tradiția timpurie apar:

- `Unitriebklasse / inäqual`;
- `biäqual`;
- `triäqual`;
- `quadriäqual`.

`Triebpathologie II` descrie explicit cazuri cu:
- `Unitriebgefahr / Unitriebklasse`;
- `Biäquale Triebklasse`;
- `Triäquale Triebklasse`;
- și structuri cu patru grade egale.

`Ich-Analyse I` folosește în cazuri formule precum `Schp− – Pe− – biäqual`.

Susan Deri (1949) explică această versiune și spune că cele 16 clase de bază sunt subdivizate după bi-/tri-/quadri-equality până la un total de **144 variații de subclasă**.

## 2. Stratul revizuit: `Gefahr-/Wurzelklassen` și `Ventil-/Symptomklassen`

În `Lehrbuch` 1972, p. 284, nota de subsol spune explicit:

> **`Mit dieser neuen Einteilung werden die sogenannten «inäqualen» und «äqualen» Benennungen der ersten Auflage des Buches hinfällig.`**

Aceasta este o suprascriere editorial-doctrinară explicită a **nomenclaturii claselor din prima ediție**.

Manualul trebuie deci să urmeze ca mecanică principală:

- 8 `Haupttriebklassen`;
- 16 `Unterklassen` semnate;
- `Gefahr-/Wurzelklassen`;
- `Ventil-/Symptomklassen`;
- `Triventilklasse / Quadriventilklasse`.

Nu trebuie să predea sistemul Deri de „144 subclasses” ca regulă canonică actuală a ediției târzii Szondi.

## 3. `Äqualität` nu dispare ca fapt descriptiv

Nota din 1972 declară depășite **denumirile de clasă** `inäqual / äqual`, nu existența egalității între grade.

Chiar în același `Lehrbuch` târziu:

- Szondi continuă să vorbească despre `biäquale oder fast biäquale Latenzgrade`;
- descrie pierderea unei `Biäqualität`;
- Schafir spune că a doua clasă devine constantă „mit der `Äqualität`”.

Prin urmare:

> **egalitatea dintre `Latenzgrade` rămâne o proprietate reală a seriei; vechea nomenclatură care transforma această egalitate într-un sistem de clase `bi-/tri-/quadriäqual` este cea explicit revizuită.**

Această distincție trebuie protejată la outline și audit.

---

# IV. `Triebklasse` este actuală și mobilă, nu o trăsătură fixă

`Lehrbuch`, pp. 288–289, control vizual, insistă asupra cuvântului **`aktuell`**.

Szondi spune că ar fi greșit să presupunem că o anumită latență nu se poate transforma în cursul vieții.

Formulările-cheie:

- `Triebklasse` indică în general `die aktuell virtuellste Konduktornatur`;
- individul poate trece dintr-o clasă în alta;
- apartenența la clasă este o situație pulsională **`relativ umweltlabil`**, nu `absolut umweltstabil`.

Studiile seriale citate de el arată schimbări de clasă între serii de o zi și serii de o lună și în cursul tratamentului.

Consecința editorială este obligatorie:

> **`Triebklasse` nu se predă ca tip de personalitate fix și nici ca identitate diagnostică stabilă. Este o organizare serială actuală a latențelor.**

Această regulă este compatibilă cu cap. 38–41: seria este mișcare, nu „profil adevărat”.

---

# V. Ce spune și ce nu spune `Triebklasse`

## 1. În doctrina lui Szondi

Sursele primare îi atribuie clasei afirmații tari:

- localizează cea mai mare `Triebgefahr`;
- indică `Wurzelfaktor` / `Konduktornatur`;
- în `Triebpathologie II`, „arată `Erkrankungsrichtung`”;
- în `Schicksalsanalyse`, `Wurzelfaktoren` sunt legați de alegerea de destin și de conductorat.

Aceste afirmații trebuie redate ca doctrină istorică, nu transformate în validare clinică contemporană.

## 2. Ce nu poate face singură

Chiar `Lehrbuch` spune că:

- toate cele patru diferențe trebuie analizate;
- clasa nu spune calitatea exactă a `Notausgänge / Triebventile`;
- această individualizare aparține `Triebformel`.

La pp. 291–292, Szondi formulează relația foarte clar:

- `Triebklasse` orientează asupra `Wurzelfaktoren`, adică asupra locului pericolului;
- `Triebformel` arată și `Symptomfaktoren`, adică ventilele / ieșirile specifice.

Aceasta este frontiera exactă între cap. 42 și cap. 43.

Formula de control pentru cap. 42:

> **clasa localizează și califică latența conducătoare; formula va descrie mai fin configurația rădăcinilor și a ieșirilor.**

---

# VI. Confirmarea din `Triebpathologie II`

`Triebpathologie II`, pp. tipărite **244–251**, oferă o formulare foarte concentrată a mecanicii și a fost controlată vizual la pp. 245–246.

Confirmă:

1. `TspG` măsoară `Entladungsbereitschaft`;
2. `TspD` măsoară cantitativ `Triebgefahr` intravectorială;
3. `Latenzgrad / Latenzgröße` localizează factorul și mărimea pericolului;
4. `Latenzproportionen` compară cele patru domenii;
5. persoana aparține clasei în care `Latenzgrad`, adică `Triebgefahr`, este cel mai mare;
6. `Triebklasse` răspunde la întrebarea: **unde este actualmente cea mai mare `Triebgefahr`?**

Exemplul:

`Schp− / 10 : Phy− / 6 : Sh+ / 6 : Cd+ / 3`

duce la `Schp−` ca clasă conducătoare.

### Limită cronologică

Același volum folosește apoi sistemul mai vechi `Uni-/Bi-/Tri-/Quadriäqual Triebklasse`.

Pentru manual, acest strat este important istoric, dar mecanica principală trebuie controlată prin revizia explicită din `Lehrbuch` 1972.

---

# VII. Tradiția ulterioară

## Susan Deri — pp. 55–60

Deri explică limpede:

- cea mai mare `degree of latency` -> `Triebklasse`;
- factorul mai latent dă indexul;
- direcția `+ / −` a factorului latent califică clasa;
- există 16 clase de bază.

Ea descrie însă și subdivizarea veche în bi-/tri-/quadri-equal până la **144** variante.

Pentru cap. 42, Deri este foarte utilă pentru pedagogia relației:

`Latenzproportion -> strongest latency -> class -> root factor`.

Dar sistemul de 144 variante trebuie marcat drept **strat al ediției vechi**, nu ca mecanică finală după revizia din `Lehrbuch` 1972.

Deri oferă și o limită metodologică valoroasă: nu recomandă interpretarea `drive-classes` și `drive-formulas` fără analiza calitativă atentă a profilelor.

## Jacques Mélon — pp. aproximativ 165–167

Mélon, 1975, confirmă tocmai sistemul revizuit:

- `TspGD` = diferența intravectorială;
- factorul cu `TspG` mai mic dă numele clasei;
- opt clase principale și 16 subclase după direcția `+ / −`;
- `TspGD > 4` -> `classe dangereuse / Gefahrklasse`;
- când toate diferențele sunt sub 5 -> `classe-soupape / Triebventilklasse`;
- descrie clase mono-, bi-, tri- sau quadripericuloase și clase triventile / quadriventile.

Mélon este confirmare ulterioară utilă pentru lectura reviziei târzii, nu autoritate peste Szondi.

---

# VIII. Pass 10/10 — relevanța fiecărei surse canonice

## 1. `SCHICKSALSANALYSE- Szondi`

Relevanță doctrinară secundară.

Confirmă:
- persoana aparține clasei trebuinței cu cel mai puternic `Latenzgrad`;
- `Wurzelfaktoren` sunt legați de `Konduktorfaktoren`;
- aceeași persoană poate avea mai multe direcții dinamice în proporții diferite.

Nu oferă mecanica revizuită completă `Gefahr/Ventil`.

## 2. `Susan Deri - Szondi Introduction`

Relevanță tehnică ulterioară ridicată, dar istorică.

Explică foarte clar cele 16 clase de bază și semnul factorului latent, dar descrie sistemul vechi bi-/tri-/quadri-equal și cele 144 variante, explicit depășit nomenclatural de `Lehrbuch` 1972.

## 3. `Szondi Ich-Analyse 1. Teil`

Relevanță doctrinară și cronologică.

Folosește în cazuri:
- `Wurzelfaktoren`;
- `Latenzproportionen`;
- `biäquale Latenz`;
- formule de clasă de tip `Schp−–Pe− biäqual`.

Este o confirmare a stratului vechi și a ideii proporționale, nu mecanica finală revizuită.

## 4. `Szondi Ich-Analyse 2. Teil`

Relevanță periferică pentru cap. 42.

Conține numeroase discuții despre `Triebgefahr` și apărare, dar nu adaugă o regulă operațională nouă pentru construirea `Triebklasse / Unterklasse`.

## 5. `Szondi Lehrbuch der experimentellen Triebdiagnostik`

**Sursa primară decisivă.**

Pp. 278–284:
- clasa din maximul de latență;
- minimul ca ventile;
- opt `Haupttriebklassen`;
- 16 `Unterklassen`;
- pragul 5/4;
- `Gefahr-/Wurzelklassen`;
- `Ventil-/Symptomklassen`;
- `Triventil / Quadriventil`;
- nota de revizie care declară vechile denumiri `inäqual / äqual` depășite.

Pp. 288–290:
- caracterul `aktuell`, mobil și relativ umweltlabil al clasei.

Pp. 291–292:
- distincția funcțională `Triebklasse` versus `Triebformel`.

## 6. `Szondi Schicksalsanalytische Therapie 1. Teil`

Relevanță doctrinar-clinică periferică.

Confirmă importanța `Triebgefahr` în dinamica apărării și terapiei, dar nu adaugă reguli de calcul sau clasificare pentru cap. 42.

## 7. `Szondi Schicksalsanalytische Therapie 2. Teil`

Nu s-a identificat material operațional nou pentru `Triebklasse / Unterklasse / Gefahrklasse / Ventilklasse` în bounded pass.

Rămâne control negativ 10/10.

## 8. `Szondi Triebpathologie 1. Teil`

Relevanță doctrinară ridicată pentru statutul clasei.

Confirmă:
- clasele sunt construite din `Triebgefahren` și `Wurzelfaktoren`;
- `Triebklasse` indică **actual** cea mai mare `Triebgefahr`;
- clasa poate fi schimbată;
- apartenența este relativ umweltlabilă.

Este foarte util pentru a împiedica reificarea clasei ca trăsătură fixă.

## 9. `Szondi Triebpathologie 2. Teil`

Relevanță primară foarte ridicată pentru definiția concentrată a clasei și pentru sensul doctrinar al `Triebgefahr`.

Păstrează însă nomenclatura mai veche `uni-/bi-/tri-/quadriäqual`, care trebuie citită cronologic față de revizia explicită din `Lehrbuch` 1972.

## 10. `The_orie_et_pratique_du_Szondi_J_Me_lon`

Relevanță tehnică ulterioară ridicată.

Confirmă sistemul revizuit:
- opt clase;
- 16 subclase;
- prag `>4` versus `<5`;
- `classes-racines / classes-soupapes`;
- triventil / quadriventil.

Rămâne tradiție ulterioară atribuită.

---

# IX. Două audit focus-uri materiale

Research-ul este suficient pentru outline, dar două puncte trebuie marcate de pe acum pentru auditul doctrinar viitor.

## AUDIT FOCUS 1 — domeniul pragului `5 / 4`

Pragul este formulat explicit pentru **`Zehnerserie`**.

Cap. 41 a stabilit că un `TspG / Latenzgrad` brut poate depăși 10 într-o serie mai lungă; exemplul cu 15 profile are `13`.

În corpusul controlat nu s-a identificat o regulă primară explicită care să spună:

- dacă pragul `5/4` se aplică neschimbat valorilor brute ale seriilor `>10`;
- sau dacă acestea trebuie aduse mai întâi la baza de zece printr-o altă operație.

Prin urmare, manualul nu trebuie să transforme `5–10 / 0–4` într-un cutoff universal independent de lungimea seriei.

Formularea sigură pentru outline:

> **pragul canonic `5–10 = Gefahr`, `0–4 = Ventil` este direct documentat pe scara `Zehnerserie`.**

## AUDIT FOCUS 2 — egalitatea la maxim și lipsa unui tie-break universal

Cap. 41 a stabilit că egalitățile rămân egalități.

`Lehrbuch` târziu:
- nu mai folosește vechile denumiri `bi-/tri-/quadriäqual` ca sistem principal de clase;
- dar continuă să folosească `Biäqualität` descriptiv;
- pentru tri-/quadriventile spune că plasarea în tabele se face după valoarea relativ cea mai mare.

Pasajul controlat nu dă însă un tie-break universal atunci când două sau mai multe valori maxime sunt exact egale.

Manualul nu va inventa unul.

---

# X. Constrângerea moștenită `CH41-SHORT-01`

Cap. 42 depinde numeric de cap. 41.

Pentru seriile de 3–9 profile, `CH41-SHORT-01` rămâne activ:

- instrucțiunile Schafir–Szondi cer `Umrechnung`;
- Fall 18 calculează `Latenzgrade` direct din valorile brute;
- nici `TspG brut -> Tabelle 13 -> TspD`, nici `TspD brut -> Tabelle 13` nu poate fi ridicat la regulă universală.

Consecința pentru cap. 42 este directă:

> **nu putem autoriza un algoritm universal de `Triebklasse / Gefahr / Ventil` pentru seria scurtă dacă însuși `Latenzgrad` de intrare nu are o ordine de conversie canonică neechivocă.**

Nu este nevoie de un HOLD nou: aceasta este o **constrângere upstream a `CH41-SHORT-01`**.

Pentru predarea algoritmului complet al cap. 42, cazul canonic sigur este `Zehnerserie`.

---

# XI. Algoritmul de lucru sigur pentru `Zehnerserie`

Pentru outline, mecanica poate fi formulată astfel:

1. pornim de la cele patru valori deja construite în cap. 41:
   `Vf± / Latenzgröße`;
2. păstrăm ordinea descrescătoare și egalitățile;
3. valoarea relativ maximă indică `Haupttriebklasse`;
4. factorul indexat este `Wurzelfaktor`-ul / factorul cu `TspG` mai mic;
5. direcția `+ / −` a acestui factor dă `Unterklasse`;
6. separat, clasificăm fiecare `Latenzgröße` pe scara de zece:
   - `>=5` -> `Triebgefahr`;
   - `<=4` -> `Triebventil`;
7. dacă există valori `>=5`, notăm câte domenii sunt `Gefahr`: una, două, trei sau patru;
8. dacă toate sunt `<=4`, intrăm în `Ventil-/Symptomklassen`:
   - amplitudine max–min `3–4` -> `Triventilklasse`;
   - amplitudine max–min `<3` -> `Quadriventilklasse`;
   - `Biventilklassen` nu sunt evidențiate separat în sistemul simplificat târziu;
9. în structurile tri-/quadriventile, orientarea în tabele se face după valoarea relativ cea mai mare, fără tie-break inventat la egalitate;
10. `Triebformel` nu se calculează aici; ea este pasul următor.

Acest algoritm trebuie prezentat explicit ca **mecanică a `Zehnerserie`**.

---

# XII. Frontiere epistemice pentru redactare

Cap. 42 nu trebuie să transforme:

- `Triebklasse` într-un tip de personalitate fix;
- `Wurzelfaktor` într-o genă demonstrată;
- `Triebgefahr` într-un scor contemporan de risc;
- `Ventil` într-un mecanism adaptativ automat;
- `Gefahrklasse` într-un diagnostic;
- `Unterklasse +/−` într-o evaluare bun/rău;
- pragul `5/4` într-o scală psihometrică modernă;
- vechiul sistem de `Äqualität` descris de Deri într-o regulă canonică actuală.

Formula clinică de control:

> **clasa organizează formal latențele unei serii; nu stabilește singură un diagnostic și nu fixează persoana într-un tip imuabil.**

---

# XIII. Delimitări față de capitolele vecine

## Față de cap. 41

Cap. 41 produce:
- `TspG`;
- `TspD`;
- `Latenzgrad / Latenzgröße`;
- `Latenzproportionen`.

Cap. 42 pornește de la aceste rezultate. Nu recalculează mecanica lor.

`CH41-SHORT-01` rămâne constrângere upstream.

## Față de cap. 43

Cap. 42 poate spune că `Triebformel` va diferenția mai fin indivizii aceleiași clase și va arăta `Symptomfaktoren / Notausgänge`.

Nu trebuie să predea:
- construcția fracției;
- numărătorul/numitorul;
- formula abreviată/completă;
- regulile exacte ale `Triebformel`.

Acestea sunt materia cap. 43.

## Față de cap. 46–52

Listele istorice de diagnostice asociate fiecărei clase, tabelele diferențial-diagnostice și `Erkrankungsrichtung` nu trebuie dezvoltate aici.

Cap. 42 fixează aparatul formal și sensul doctrinar minim. Aplicarea clinică va fi reluată în partea a VIII-a.

---

# XIV. Achiziții care trebuie protejate în outline

1. `Triebklasse` pornește din **maximul relativ al `Latenzgrad`-ului**.
2. Există **8 `Haupttriebklassen`**: `Sh, Ss, Pe, Phy, Schk, Schp, Cd, Cm`.
3. Factorul din numele clasei este factorul cu **`TspG` mai mic** — `Wurzelfaktor` în doctrina lui Szondi.
4. Semnul `+ / −` al `Wurzelfaktor`-ului produce **16 `Unterklassen`**.
5. `Unterklasse +/−` nu este semnul lui `TspD` și nu este evaluare morală.
6. `Hauptklasse/Unterklasse` = **axa relativă**.
7. `5–10 = Triebgefahr`, `0–4 = Triebventil` = **axa absolută a `Zehnerserie`**.
8. `Ventil` nu înseamnă sănătos; este `Notausgang` / canal de descărcare și poate fi simptomatic.
9. Pot exista una, două, trei sau patru `Gefahren`.
10. Dacă toate patru valorile sunt sub 5, late `Lehrbuch` folosește `Ventil-/Symptomklassen`.
11. `Triventilklasse`: amplitudine `3–4`.
12. `Quadriventilklasse`: amplitudine `<3`.
13. `Biventilklasse` nu mai este evidențiată separat în simplificarea târzie.
14. Vechile denumiri `inäqual / bi-/tri-/quadriäqual` ale primei ediții sunt declarate explicit `hinfällig` de `Lehrbuch` 1972.
15. `Äqualität` rămâne însă o proprietate descriptivă a egalității între grade; nu inventăm tie-break.
16. Deri descrie sistemul vechi de 144 variante; el este istoric, nu mecanica finală.
17. Mélon confirmă sistemul târziu `Gefahr/Ventil`.
18. `Triebklasse` este **actuală, mobilă, relativ umweltlabilă**; persoana poate schimba clasa.
19. Clasa nu suspendă întreaga `Latenzproportion`; toate cele patru valori rămân relevante.
20. `Triebklasse` localizează `Wurzelfaktoren`; `Triebformel` va individualiza și `Symptomfaktoren / Ventile`.
21. Pragul `5/4` nu se universalizează necritic la valori brute din serii mai lungi de zece.
22. `CH41-SHORT-01` împiedică autorizarea unui algoritm universal pentru clasele seriilor 3–9.
23. `Triebklasse ≠ diagnostic ≠ tip fix de personalitate`.

---

# XV. Concluzie de research

Nucleul cap. 42 este canonic stabil pentru `Zehnerserie`:

`Latenzproportionen -> maxim relativ -> Haupttriebklasse -> Wurzelfaktor + direcție -> Unterklasse`

și, pe o axă distinctă:

`Latenzgröße 5–10 -> Triebgefahr`  
`Latenzgröße 0–4 -> Triebventil`.

Cea mai importantă achiziție a deep pass-ului este cronologică: **sistemul vechi `inäqual / bi-/tri-/quadriäqual`, prezent în Deri și în unele volume Szondi, nu trebuie predat ca mecanică finală**, deoarece `Lehrbuch` 1972 spune explicit că aceste denumiri ale primei ediții devin caduce prin noua împărțire în `Gefahr-/Wurzelklassen` și `Ventil-/Symptomklassen`.

Cele două puncte care trebuie urmărite la audit sunt:

1. domeniul exact al pragului `5/4`, documentat explicit pe scara `Zehnerserie`, nu ca prag brut universal pentru orice lungime de serie;
2. lipsa unui tie-break universal pentru maxime perfect egale.

`CH41-SHORT-01` rămâne constrângere upstream pentru seriile 3–9.

**Următorul pas: CH42 DRAFT.**
