# Capitolul 13 — Reader pass / remediere punctuală

**Capitol:** Reacțiile factoriale: `+`, `−`, `±`, `0`  
**Verdict:** PASS AFTER TARGETED DOCTRINAL REMEDIATION  
**Statut rezultat:** STABLE DRAFT

## Observația autorului integrată

Cross-check-ul cu `Lehrbuch` Tabelle 3 și cu implementarea formală din `main` a identificat o problemă locală, nu structurală:

- `4/2` fusese clasificat greșit ca `+`;
- `2/4` fusese clasificat greșit ca `−`.

Corecția matură este:

- `4/2 → ±!`;
- `2/4 → ±!`.

## Alte corecții integrate

- ansamblul este numit „cele patru forme/reacții factoriale de bază”;
- `direcție/tendință` este rezervat pentru `+ / − / ±`;
- `0` este tratat ca `Nullreaktion`, nu ca o a patra direcție;
- afirmația despre concordanța completă Szondi–Deri a fost retrasă; pentru pragurile exacte, Tabelle 3 are prioritate.

## Elemente protejate

- „`+` și `−` exprimă dominanțe, nu puritate”;
- `Remanenz der Opposition`;
- „`0` nu înseamnă că factorul lipsește din persoană”;
- finalul `2/0` versus `6/0`, extins acum prin cazul `4/2 → ±!`.

## Propagare înainte

- cap. 14 trebuie să distingă strict `Quantumspannung` de `Tendenzspannungsgrad (TspG)`;
- `0` liber și `ø` (`Zwangs-Nullreaktion`) rămân distincte pentru capitolele despre complement și serie.

Nu este necesară o nouă rescriere structurală a cap. 13.