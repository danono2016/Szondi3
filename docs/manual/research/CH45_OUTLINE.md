# CH45 — Outline

**Capitol:** 45 — `Dur–Moll` și `Sozialindex`: metode proporționale și limite  
**Statut:** OUTLINE COMPLETE — RESEARCH CLOSED / DRAFT NEXT / CH45-AUDIT-01 ACTIVE / CH45-AUDIT-02 ACTIVE / CH45-THKP-01 SOURCE LIMIT / INTERNAL TENSION HOLD ACTIVE  
**Ramură:** `manual`

---

## Întrebarea cognitivă

**Cum transformă Szondi o serie de `Vektorbilder` în două proporții globale — `Dur–Moll / Sexualindex` și `Sozialindex` — fără ca aceste proporții istorice să fie confundate cu un diagnostic, cu o normă psihometrică actuală sau cu o sentință socială?**

## Teza capitolului

Capitolul trebuie să arate că cele două proceduri sunt `Proporzmethoden`: metode empirice și parțiale care clasifică `Vektorbilder` ale seriei în două clase și calculează raportul dintre ele. Ele folosesc aceeași materie serială, dar nu aceeași grilă și nu aceeași regulă pentru `Quantumspannung`.

Formula de control:

> **`Dur–Moll` și `Sozialindex` clasifică proporțional `Vektorbilder` ale unei serii după două grile istorice ale lui Szondi; ele nu transformă proporția într-o normă atemporală, într-un diagnostic sau într-o sentință socială.**

---

# Arhitectura draftului

## 1. Deschiderea: după indicii factoriali, două grile asupra `Vektorbild`-ului

Funcție:

- leagă natural cap. 44 de cap. 45;
- arată schimbarea materiei de calcul: de la totaluri factoriale `0 / ±` la clasificarea întregului `Vektorbild`;
- introduce termenul comun `Proporzmethoden`;
- fixează de la început caracterul parțial și empiric al celor două metode.

De evitat:

- prezentarea lor ca indici care rezumă întreaga persoană;
- confundarea cu `TspQu`, `% Sy-Re`, `Latenzproportionen` sau `Tabelle 13`;
- introducerea imediată a interpretărilor clinice înaintea mecanicii.

## 2. Ce înseamnă `Proporzmethode`

Funcție:

- explică faptul că fiecare metodă repartizează fiecare `Vektorbild` într-una din două clase;
- arată schema comună:

`serie -> Vektorbilder -> clasificare bipolară -> frecvențe + Quantumspannungen -> proporție / index`;

- precizează că Szondi consideră aceste metode capabile să lumineze anumite direcții ale dezvoltării psihosexuale sau sociale, dar nu `Schicksalsplan`-ul complet.

Protecție:

> **Metodă proporțională ≠ metodă totală de interpretare.**

## 3. Cronologia Dur–Moll: metoda factorială din 1952

Funcție:

- introduce stratul vechi din `Triebpathologie I`;
- explică faptul că în 1952 opoziția Dur/Moll era aplicată factorial celor opt factori;
- consemnează calculul vechi `Σ Durreaktionen : Σ Mollreaktionen`;
- poate folosi Fall 5 (`10 Dur : 41 Moll = 1 : 4,1`) numai pentru a face vizibilă mecanica istorică.

Statut:

- metodă primară Szondi, dar cronologic anterioară;
- nu rămâne protocol paralel după corecția matură din `Lehrbuch`.

## 4. De ce Szondi își corectează metoda

Funcție:

- prezintă critica explicită din `Lehrbuch`: descompunerea unor `Vektorbilder` frecvente în reacții factoriale separate producea o deplasare artificială a proporției;
- explică trecerea de la reacții factoriale la `Vektorreaktionen / Vektorbilder`;
- fixează prioritatea tehnică a metodei mature.

Formula de control:

> **Metoda factorială explică geneza conceptului; metoda vectorială din `Lehrbuch` guvernează calculul matur.**

## 5. Domeniul metodei Dur–Moll mature

Funcție:

- păstrează formularea direct documentată `Achter- oder Zehnerserie`;
- spune clar că protocolul matur este documentat pentru 8 sau 10 profile;
- nu extinde automat regula la 9 sau la 3–7 profile;
- nu importă `Tabelle 13`.

Protecție:

> **Domeniul direct documentat al protocolului Dur–Moll matur este 8 sau 10 profile.**

## 6. Matricea Dur–Moll — `Abb. 21`

Funcție:

- introduce cele 16 `Vektorbilder` standard pentru fiecare vector;
- reproduce în draft matricea canonică D/M controlată vizual;
- face vizibil faptul că în fiecare vector există exact 8 forme D și 8 forme M;
- nu deduce clasa din semne izolate, ci folosește matricea tipărită.

### CH45-AUDIT-01 — ACTIVE

Auditul doctrinar trebuie să reconstruiască independent, celulă cu celulă, toate cele 64 de clasificări din `Abb. 21`.

## 7. Cum intră `Quantumspannung` în Dur–Moll

Funcție:

- arată că `!` nu schimbă clasa D/M a `Vektorbild`-ului;
- fiecare `!` adaugă o unitate aceleiași clase căreia îi aparține Vektorbild-ul;
- formulează mecanica explicit:

`D ponderat = nr. Vektorbilder D + nr. ! din Vektorbilder D`

`M ponderat = nr. Vektorbilder M + nr. ! din Vektorbilder M`.

Protecție:

- regula este locală metodei Dur–Moll;
- nu se transformă într-o nouă semnificație generală a lui `!`.

## 8. De la proporțiile vectoriale la `Sexualindex / Durindex`

Funcție:

- poate arăta mai întâi raportul local D:M pe vector;
- apoi trece la totalul global;
- predă formula:

`%Dur = (ΣD × 100) / (ΣD + ΣM)`

`%Moll = 100 − %Dur`;

- explică faptul că în tradiția primară ulterioară `Sexualindex` este exprimat prin `Durindex`.

De separat:

- proporția D:M pe vector;
- totalul global D:M;
- procentul global `Sexualindex / Durindex`.

## 9. Ce înseamnă istoric Dur și Moll

Funcție:

- redă vocabularul lui Szondi: Dur ca tare/rece și Moll ca moale/cald, cu legăturile istorice masculin/feminin;
- păstrează motivul denumirii muzicale, care depășește simpla înregistrare somatică;
- consemnează rangul psihosexual istoric revendicat de Szondi;
- nu traduce aceste categorii în concepte contemporane de gen, identitate sau orientare sexuală.

Reper primar de păstrat:

- la bărbatul considerat normal: `D:M = 2:1`;
- la femei: predominanță Moll, fără inventarea unei simetrii obligatorii exacte `1:2` dacă sursa primară nu o formulează astfel.

Auditul trebuie să protejeze această asimetrie de formulare.

## 10. Limita internă `Zeitmoral`

Funcție:

- introduce devreme limita primară care guvernează și `Sozialindex`;
- păstrează literal formula doctrinară esențială:

`Beide Proporzmethoden verwandeln sich, wenn die Zeitmoral sich verwandelt.`

Consecință editorială:

- reperele și clasificările sunt istorice;
- chiar Szondi le declară sensibile la transformarea moralei epocii;
- manualul nu le prezintă ca norme atemporale.

Această secțiune trebuie să prevină, nu să repete, toate balustradele ulterioare.

## 11. Intrarea în `Sozialindex`: altă grilă asupra acelorași Vektorbilder

Funcție:

- trece de la polaritatea D/M la clasificarea `sozialpositiv / sozialnegativ`;
- explică ancorarea istorică a clasificării în normele familiei, sexualității, religiei, proprietății, muncii și conformității sociale;
- precizează că aceste etichete aparțin vocabularului normativ istoric al metodei.

Proveniență:

- formularea sigură în draft: **metoda prezentată de Szondi, construită cu participarea unui colaborator**;
- nu face din tensiunea dintre anonimatul colaboratorului și atribuirea figurii o problemă centrală a capitolului, dacă auditul nu cere altceva.

## 12. Domeniul `Sozialindex`

Funcție:

- păstrează formularea directă `acht bis zehn Profile`;
- separă domeniul de cel Dur–Moll:
  - Dur–Moll matur: 8 sau 10;
  - `Sozialindex`: 8–10;
- nu uniformizează cele două instrucțiuni;
- nu introduce `Tabelle 13` pentru seriile mai scurte.

## 13. Matricea socială — `Abb. 24`

Funcție:

- reproduce în draft matricea canonică `sozialpositiv / sozialnegativ` controlată vizual;
- păstrează toate cele 64 de celule;
- arată că fiecare vector conține exact 8 forme pozitive și 8 negative;
- clasa se citește din matricea vectorială, nu dintr-un dicționar al semnelor factoriale.

### CH45-AUDIT-02 — ACTIVE

Auditul trebuie să recontroleze independent `Abb. 24`, formula și tratamentul `Quantumspannung`.

## 14. Regula specială pentru `!` în `Sozialindex`

Funcție:

- marchează explicit ruptura față de Dur–Moll;
- toate `Quantumspannungen !` se adaugă istoric clasei `sozialnegativ`, indiferent de clasa inițială a Vektorbild-ului;
- predă:

`soz− ponderat = nr. Vektorbilder soz− + Σ!`

`soz+ ponderat = nr. Vektorbilder soz+`.

Protecție absolută:

> **Regula `! -> sozialnegativ` este o regulă istorică a `Sozialindex`, nu o definiție clinică generală a `Quantumspannung` și nu un scor contemporan de periculozitate.**

## 15. Formula `Sozialindex`

Formula:

`Sozialindex = (soz+ × 100) / (soz+ + soz−)`

unde `soz−` include deja `Σ!`.

Se poate menționa forma compactă echivalentă:

`soz+ × 100 / (soz+ + soz− + Σ!)`

numai dacă se precizează clar că aici `Σ!` nu fusese încă inclus în `soz−`.

Funcție:

- face calculul transparent;
- previne dubla adăugare a `Σ!`;
- separă formula de interpretare.

## 16. Reperele istorice ale `Sozialindex`

Repere de păstrat:

- `Normalzone` istorică aproximativ `40–50%`;
- valori `>80%` descrise ca rare;
- pragul de `40%` tratat de Szondi drept mărime critică pentru socialitate;
- chiar Szondi precizează că `<40%` nu dovedește automat o faptă criminală.

Funcție:

- redă revendicarea istorică fără s-o transforma în normă psihometrică actuală;
- separă `sozialnegativ / asozial` în vocabularul lui Szondi de diagnosticul, moralitatea și riscul contemporan.

## 17. De ce trebuie citite sinoptic `Sexualindex` și `Sozialindex`

Funcție:

- păstrează avertismentul lui Szondi că Dur–Moll nu trebuie folosit singur pentru aprecierea socială;
- explică faptul că cei doi indici răspund la grile diferite și nu se deduc unul din altul;
- folosește câteva exemple istorice numai pentru a arăta independența relativă a celor două axe.

De evitat:

- construirea unui algoritm de personalitate din combinațiile procentuale;
- transformarea combinațiilor istorice în clasificator clinic modern.

## 18. `Tabelle 18`: material empiric preliminar

Funcție:

- introduce tabelul ca sinteză de grupuri/cazuri, nu ca standard normativ final;
- păstrează câteva valori reprezentative numai dacă servesc argumentului;
- protejează valorile sensibile controlate vizual, inclusiv `85,0` pentru psihoigienist;
- consemnează declarația lui Szondi că pentru corelații finale ar fi necesare multe sute de serii suplimentare.

Formula epistemică:

> **`Tabelle 18` ilustrează stadiul empiric al metodei; nu îl transformă într-un etalon psihometric final.**

## 19. `Hintergänger / ThKP`: limita internă reală a sursei

Funcție:

- redă scurt pasajul exploratoriu în care Szondi sugerează calculul `Sozialindex` în ThKP;
- îl confruntă cu declarația ulterioară că experiența interpretativă pentru cele două `Proporzmethoden` în Hintergänger lipsește încă;
- refuză arbitrajul editorial.

### CH45-THKP-01 — SOURCE LIMIT / INTERNAL TENSION HOLD ACTIVE

Regulă pentru draft:

> **Corpusul permite consemnarea unei sugestii exploratorii, nu predarea unei reguli generale validate pentru ThKP.**

Nu se folosește pasajul pentru:

- atribuirea autoratului unei infracțiuni;
- predicție medico-legală;
- inferență contemporană de risc;
- extinderea automată a ambelor Proporzmethoden la Hintergänger.

## 20. Deri, Mélon și straturile ulterioare

Secțiune scurtă.

- Deri nu furnizează mecanica matură completă a celor doi indici;
- Mélon confirmă ulterior structura generală, inclusiv `Sexualindex`, clasificarea vectorială, toate `!` negative în `Sozialindex` și zona 40–50;
- simplificarea ulterioară `1D:2M` la femei nu suprascrie formularea primară mai prudentă;
- `Lehrbuch` rămâne sursa tehnică principală.

## 21. Protocol sigur Dur–Moll

Pentru domeniul direct documentat:

1. stabilește lungimea seriei și verifică dacă este 8 sau 10 profile;
2. pentru fiecare vector din fiecare profil, identifică `Vektorbild`-ul;
3. clasifică-l D sau M exclusiv după `Abb. 21`;
4. numără `!` din Vektorbilder D și M;
5. adaugă fiecare `!` clasei D/M a Vektorbild-ului;
6. obține totalurile ponderate `ΣD` și `ΣM`;
7. calculează `%Dur = ΣD × 100 / (ΣD + ΣM)`;
8. calculează `%Moll = 100 − %Dur`;
9. citește rezultatul numai în contextul istoric și configurațional al seriei.

Protocolul nu inventează conversie pentru 3–7 sau pentru 9 profile.

## 22. Protocol sigur `Sozialindex`

Pentru 8–10 profile:

1. identifică toate `Vektorbilder` din serie;
2. clasifică fiecare formă +/− exclusiv după `Abb. 24`;
3. numără frecvențele `soz+` și `soz−`;
4. numără toate `Quantumspannungen !`;
5. adaugă toate `!` la totalul `soz−`;
6. calculează `Sozialindex = soz+ × 100 / (soz+ + soz−)`;
7. citește rezultatul împreună cu `Sexualindex` și cu seria calitativă;
8. păstrează caracterul istoric al grilei și limita `Zeitmoral`.

Nu se importă `Tabelle 13` și nu se transformă procentul în verdict social.

## 23. Închiderea Părții VII

Funcție:

- nu recapitulează toate capitolele 38–45;
- face vizibil pragul pedagogic al părții:

`profil -> serie -> calcul formal -> limită a calculului`;

- spune că, după CH45, cititorul poate organiza și calcula formal seria, dar nu a ajuns încă la diagnostic clinic;
- pregătește trecerea spre cap. 46, unde apare `Experimentelles Syndrom` și lanțul clinic controlat.

Tranziția trebuie să producă nevoia capitolului următor: calculele pot ordona o serie, dar încă nu spun cum devine o constelație formală o ipoteză clinică.

---

# Achiziții PROTECT pentru DRAFT v1

- `Dur–Moll` și `Sozialindex` = `Proporzmethoden`, metode parțiale și empirice;
- metoda Dur–Moll factorială din 1952 rămâne distinctă de metoda vectorială matură din `Lehrbuch`;
- `Lehrbuch` are prioritate pentru protocolul matur;
- Dur–Moll matur este documentat direct pentru `Achter- oder Zehnerserie`;
- `Sozialindex` este documentat direct pentru 8–10 profile;
- `Tabelle 13` nu se importă fără sursă;
- `Abb. 21` și `Abb. 24` se reproduc după control vizual canonic, nu din deducții factoriale;
- fiecare vector are 8 forme într-o clasă și 8 în cealaltă în ambele matrici;
- în Dur–Moll, `!` întărește clasa D/M a Vektorbild-ului;
- în `Sozialindex`, toate `!` intră istoric în clasa social-negativă;
- `%Dur = ΣD × 100 / (ΣD + ΣM)` și `%Moll = 100 − %Dur`;
- `Sozialindex = soz+ × 100 / (soz+ + soz−)` după includerea `Σ!` în `soz−`;
- `D:M = 2:1` la bărbatul considerat normal rămâne reper istoric; pentru femei nu se inventează simetrie obligatorie exactă dacă sursa primară nu o formulează;
- `40–50%` și pragul `40%` pentru `Sozialindex` rămân repere istorice, nu norme psihometrice contemporane;
- `Beide Proporzmethoden verwandeln sich, wenn die Zeitmoral sich verwandelt.` rămâne limită doctrinară internă centrală;
- `Tabelle 18` rămâne material preliminar, nu standard final;
- lectura sinoptică `Sexualindex + Sozialindex` nu devine algoritm diagnostic;
- `CH45-AUDIT-01` și `CH45-AUDIT-02` rămân ACTIVE până la audit extern;
- `CH45-THKP-01` rămâne SOURCE LIMIT / INTERNAL TENSION HOLD ACTIVE;
- nicio formulă nu primește dreptul de a suspenda profilul, seria sau contextul clinic.

---

# Ce NU intră în DRAFT v1

- o regulă Dur–Moll pentru 3–7 sau 9 profile;
- conversie prin `Tabelle 13` pentru aceste metode;
- o matrice dedusă din semne factoriale în locul matricelor canonice;
- echivalarea Dur/Moll cu categorii contemporane de gen sau orientare;
- echivalarea `sozialnegativ` cu criminalitate, periculozitate ori risc actual;
- folosirea `!` ca marker clinic general de antisocialitate;
- o normă psihometrică actuală 40–50%;
- o regulă generală pentru `ThKP`;
- predicție medico-legală;
- diagnostic obținut direct din procent;
- material clinic detaliat care aparține părții a VIII-a.

---

# Gate pentru DRAFT v1

DRAFT-ul poate fi scris numai dacă păstrează simultan:

1. cronologia factorial vechi -> vectorial matur;
2. cele două domenii de serie distincte;
3. mecanica exactă a celor două matrici;
4. tratamentul diferit al `Quantumspannung`;
5. formulele procentuale;
6. `Zeitmoral` ca limită primară internă;
7. statutul preliminar al `Tabelle 18`;
8. CH45-THKP-01 fără arbitraj inventat;
9. frontiera clară dintre aparat formal și clinică.

**Următorul pas autorizat:** CH45 — DRAFT v1.
