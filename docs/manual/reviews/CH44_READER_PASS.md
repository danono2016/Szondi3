# CH44 — Reader / style pass

**Capitol:** 44 — `TspQu` și `% Sy-Re`: indicii seriei  
**Versiune evaluată inițial:** DRAFT v2, blob SHA `0ff25fa5c3f376f2ec522e74a6a5c50a440b6fe3`  
**Versiune re-evaluată:** DRAFT v3, blob SHA `f6b32a0ed4c51ab2f5c572d2a82cefae06b7296c`  
**Verdict extern inițial:** **PASS WITH MODERATE REVISION — REVISION REQUIRED BEFORE STABLE.**  
**Verdict extern final:** **STYLE PASS — READY FOR STABLE DRAFT.**  
**Statut final:** STABLE DRAFT — DOCTRINAL PASS / SCIENTIFIC AUDIT CLOSED / STYLE PASS / READER PASS CLOSED / CH44-AUDIT-01 CONFIRMED/CLOSED / CH44-AUDIT-02 CONFIRMED/CLOSED / CH44-ROUND-01 SOURCE/PROCEDURE HOLD ACTIVE  
**Ramură:** `manual`

---

## Rezultatul reader pass-ului extern pe DRAFT v2

Readerul extern a confirmat că arhitectura și mecanica sunt solide, dar a cerut o revizie moderată înainte de stabilizare. Problema identificată nu a fost doctrinară, ci stilistică: acumularea de precauții, repetarea limitelor și fragmentarea prin secțiuni foarte scurte făceau cei doi indici să pară mai complicați decât sunt ca operație.

Verdictul inițial nu acorda `STYLE PASS` și nu autoriza încă `STABLE DRAFT`.

## Elemente PROTECT

Revizia trebuia să păstreze integral:

- `TspQu = Σ0 / Σ±`;
- `% Sy-Re = ((Σ0 + Σ±) × 100) / (8 × N)`;
- opoziția `TspQu` = cum se împart `0/±` versus `% Sy-Re` = cât reprezintă împreună în serie;
- statutul formal al `Symptomreaktionen`;
- relația derivată `Σ_f TspG(f) = Σ0 + Σ±` și faptul că nu este formulă atribuită textual lui Szondi;
- exemplul `30/10` versus `20/20`;
- cazul canonic `31/0 = ∞` și separarea de `0/0` nedefinit;
- cele trei repere distincte `1–3`, `1,6–2,5`, `1,5–2,5`, fără normă unică;
- avertismentul că mărimea `TspQu` nu permite singură deducerea conduitei;
- denominatorul real `8×N`, inclusiv `64` la 8 profile și `56` la 7;
- separarea de `Tabelle 13`;
- reperul `20–30%` și termenul `vorderhand`;
- exemplul `26/28 ≈ 0,9` împreună cu `54/80 = 67,5%`;
- Fall 62 (`23/12 -> 1,9`; `35/80 -> 43,7%`);
- toate exemplele care fundamentează `CH44-ROUND-01`;
- `TspQu al unei serii ≠ curba TspQu între serii ≠ Inkonstanzmethode`;
- rangul psihodiagnostic istoric și integrarea în `Syndromanalyse`, fără transformare în validare contemporană;
- frontiera către `Dur–Moll / Sozialindex`.

## Revizia integrată în DRAFT v3

Au fost integrate conservator următoarele intervenții:

- formula metapedagogică din deschidere a fost naturalizată la „cele două formule se citesc împreună, fără a fi confundate”;
- lista tehnică `TspQu ≠ ...` a fost condensată într-o propoziție continuă;
- secțiunea despre `TspG` a fost păstrată doctrinar intactă;
- pasajul `31/0 = ∞` / `0/0` a fost păstrat intact în substanță;
- cele trei benzi `TspQu` au fost păstrate distinct, iar concluzia anti-armonizare a fost redusă la o singură formulă;
- explicația `% Sy-Re` și lungimea reală a seriei au fost reunite într-un singur flux: formulă -> `33/80` -> `8×N` -> `64/56` -> diferența față de `Tabelle 13`;
- referința administrativă la „conflictele din capitolele 41 și 43” a fost naturalizată în voce de carte;
- balustradele din jurul benzii `20–30%` au fost compactate, păstrând `vorderhand` și limita diagnostică primară;
- Fall 62 formulează direct neuniformitatea preciziei zecimale, fără voce de auditor;
- secțiunea despre precizia numerică a fost păstrată integral ca substanță și `CH44-ROUND-01` rămâne activ în memoria operațională;
- `Jahreskurve` și rangul psihodiagnostic au fost compactate fără pierderea distincției față de `Inkonstanzmethode` și fără diminuarea rangului istoric;
- protocolul de opt pași a fost păstrat;
- finalul a fost condensat la rangul istoric în `Syndromanalyse` și formula: `Calculul comprimă frecvențe ale seriei; nu înlocuiește geometria și contextul clinic.`

## Control de interferență doctrină–stil

Revizia nu modifică formulele, cifrele, exemplele canonice, atribuirea reperelor istorice, statutul `vorderhand`, denominatorul `8×N`, separarea de `Tabelle 13`, rangul `Jahreskurve` sau limita de rotunjire.

`CH44-AUDIT-01` și `CH44-AUDIT-02` rămân CONFIRMED/CLOSED. `CH44-ROUND-01` rămâne SOURCE/PROCEDURE HOLD ACTIVE și nu este cosmetizat sau rezolvat prin stil.

---

## Recheck stilistic extern pe DRAFT v3

Recheck-ul extern a fost integral și a verificat DRAFT v3 împotriva reader pass-ului anterior și a listei `PROTECT`.

Verdictul final este:

**STYLE PASS — READY FOR STABLE DRAFT.**

Readerul a confirmat explicit că revizia simplifică lectura fără să simplifice doctrina. Nu au fost identificate regresii tehnice sau stilistice blocking. Au fost reconfirmate, între altele:

- opoziția `TspQu` / `% Sy-Re`;
- relația derivată cu `TspG`;
- `31/0 = ∞` și `0/0` nedefinit;
- cele trei repere `1–3`, `1,6–2,5`, `1,5–2,5` fără normă unică;
- denominatorul real `8×N` și separarea de `Tabelle 13`;
- `20–30%` cu `vorderhand`;
- exemplele `26/28 ≈ 0,9`, `67,5%` și Fall 62;
- neuniformitatea canonică a preciziei zecimale;
- `TspQu al unei serii ≠ curba TspQu între serii ≠ Inkonstanzmethode`;
- rangul psihodiagnostic istoric în `Syndromanalyse`;
- formula finală: `Calculul comprimă frecvențe ale seriei; nu înlocuiește geometria și contextul clinic.`

Micro-șlefuirile privind frecvența termenului `quotient` și o eventuală comprimare suplimentară Deri/Mélon au fost declarate facultative și non-blocking.

## Verdict operațional final

**CH44 DRAFT v3 — STYLE PASS / READER PASS CLOSED / READY FOR STABLE DRAFT.**

Capitolul poate fi sincronizat la `STABLE DRAFT`. `CH44-ROUND-01` rămâne SOURCE/PROCEDURE HOLD ACTIVE după stabilizare: stabilitatea capitolului rezultă din documentarea fidelă a neuniformității sursei, nu din rezolvarea ei.

## Următorul pas autorizat după sincronizare

**CH45 — RESEARCH.**
