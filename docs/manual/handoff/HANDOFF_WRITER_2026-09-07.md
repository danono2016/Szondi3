# HANDOFF — SCRIITOR PRINCIPAL / Manualul Szondi

**Data:** 2026-09-07  
**Repository:** `danono2016/Szondi3`  
**Ramură obligatorie:** `manual`  
**Rol:** continuitatea de autor, cercetare doctrinară și coordonare editorială a proiectului **Manualul Szondi**.

> Acest handoff este operațional și subordonat repository-ului. `MANUAL_FOUNDATION.md` rămâne singura fundație normativă, iar `CURRENT_STATE.md` rămâne memoria operațională a stării.

## Bootstrap obligatoriu pentru chat-ul succesor

Înainte de orice acțiune citește, din ramura `manual`, în această ordine:

1. `docs/manual/CURRENT_STATE.md`;
2. `docs/manual/MANUAL_FOUNDATION.md`;
3. `docs/manual/BOOK_ARCHITECTURE.md`;
4. numai documentele active indicate de `CURRENT_STATE.md`.

La momentul acestui handoff, active sunt:

- `docs/manual/research/CH37_RESEARCH.md`;
- `docs/manual/research/CH37_OUTLINE.md`;
- `docs/manual/chapters/37_Vorderganger_si_Hinterganger_complementul_teoretic_complementul_experimental_si_dialectica_Eului_DRAFT.md`;
- `docs/manual/reviews/CH37_DOCTRINAL_PASS.md`.

Nu lucra pe `main`, nu modifica software-ul și nu reconstrui proiectul din conversații. Repository-ul este memoria operațională.

## Starea exactă la predare

Cap. 1–36 sunt `STABLE DRAFT`.

Cap. 37 este:

**DRAFT v2 / THREE MATERIAL ADDITIONS/QUALIFICATIONS INTEGRATED / SCIENTIFIC RECHECK REQUIRED.**

Nu există încă `DOCTRINAL PASS` final. Reader pass-ul stilistic este blocat. Cap. 38 este închis.

Ultimul audit extern a validat nucleul cap. 37, dar a cerut și DRAFT v2 a integrat patru intervenții care trebuie reverificate:

1. forma matură a `Komplementmethode` este serială: 8–10 VGP cu ThKP și EKP corespunzătoare; demonstrația pe un singur moment nu este protocolul complet;
2. la `VGP 0 -> ThKP ±`, Szondi distinge `Bitendenz im Hintergrund` și `Reintegration nach der vordergründigen Befriedigung`;
3. `Vordergrund ≠ Bewusstsein` și `Hintergrund ≠ Unbewusstes`;
4. tabelul `+ -> −`, `− -> +`, `± -> 0`, `0 -> ±` este direcțional; încărcările nu se șterg automat în complementarea concretă, existând exemple de tip `+! -> −!`.

Recheck-ul cerut este semn-cu-semn și profil-cu-profil, în special pentru `0 -> ±`, `0 / Ø`, încărcări, VGP/ThKP/EKP în forma serială matură și distincția conștient/inconștient.

## Protecții CH37

- `Komplementmethode` este metodă calitativ-dialectică;
- VGP / ThKP / EKP au statute epistemice diferite;
- VGP este prim-plan testologic, nu conștientul și nu o mască;
- ThKP este construcție formală, nu a doua măsurare;
- EKP provine din `Nachwahl`;
- `VGP + ThKP -> Ganzprofil` este operație formală;
- `ThKP = wirklicher Hintergänger` este interpretarea doctrinară a lui Szondi, distinctă de calcul;
- VGP și ThKP sunt concepute cu `Wirkungssimultaneität`;
- `Ganzprofil` testologic ≠ totalitatea persoanei;
- `Vorder-/Hintergänger` operează la nivelul profilului întreg, `Vorder-/Hinter-Ich` în Sch;
- complementaritate ≠ integrare / Pontifex;
- EKP nu este al treilea profil autonom de același rang în `Komplementmethode`;
- EKP se evaluează prin `Konkordanzanalyse`: concordanță VGP, concordanță ThKP sau `Neuorientierung`;
- înainte de interpretarea EKP se verifică `Wahlzwang`;
- canonic: **`Ø = Zwangsnullreaktion`**; `0 ≠ Ø`; `Ø` nu se interpretează;
- unele `Quantumspannungen` EKP pot fi numeric constrânse;
- Hintergänger nu este un sine adevărat fix, nu este sinonim cu Jung `Schatten`, iar `Hintergrund` nu este automat `Verdrängung`;
- afirmațiile prognostice tari rămân doctrină istorică, nu predicție clinică contemporană certă.

## Regula de lucru cu sursele

Corpusul canonic este 10/10: cele opt volume primare Szondi + Susan Deri + Jean Mélon. Szondi are prioritate doctrinară; Deri și Mélon sunt tradiție ulterioară explicit atribuită.

Pentru semne, formule, tabele, intensități, `0 / Ø`, `! / !! / !!!`, figuri și numere, **PDF-ul canonic este arbitrul vizual**. OCR-ul, codul și software-ul pot localiza, nu decid forma exactă.

Fiecare capitol nou pornește de la o întrebare cognitivă reală și face research 10/10. Pentru familii transversale, instabile cronologic sau bogate în excepții, se face `bounded deep corpus pass`. Deep pass-ul se oprește la saturație defensabilă și poate produce o singură propoziție în manuscris.

Principiul de autor:

> **Understand Szondi first. Then write the book without following his text. Return to the source to verify fidelity.**

Sursele furnizează doctrina, nu arhitectura prozei. Nu copia recognoscibil ordinea, subtitlurile, demonstrația sau succesiunea exemplelor.

## Regula epistemică

Păstrează separate:

`observație -> operație formală -> doctrină Szondi -> tradiție ulterioară -> explicația manualului -> inferență clinică -> diagnostic -> incertitudine`.

Formula clinică permanentă:

`constelație -> sindrom experimental -> afinitate structurală -> ipoteză clinică -> verificare clinică/anamnestică -> incertitudine`.

Nu transforma posibilitatea în fapt, cazul în lege, calculul în diagnostic, profilul în persoana totală sau teoria istorică în psihologie contemporană validată.

## Vocea și stilul

Direcția permanentă:

**Română firească, dar atmosferă szondiană.**

**Clarificăm fără să domesticim. Traducem fără să contemporaneizăm doctrina.**

Ordinea:

**1. întâi Szondi; 2. apoi claritate; 3. apoi eleganță.**

Păstrează germana când poartă o distincție reală. Nu importa tacit attachment/coping/self-regulation/mentalization și nu neutraliza vocabularul istoric doar ca să sune contemporan.

Reader pass-urile 32–36 au arătat că stilul bun reduce balustradele, metadiscursul și repetițiile după ce distincția este deja clară. Nu tăia cronologia, excepția sau termenul tehnic care poartă doctrina.

## Regula auditorilor

Fluxul este strict:

1. audit științific/doctrinar extern;
2. reader pass stilistic extern.

`PASS WITH...` nu este `DOCTRINAL PASS`. Nu închide singur auditul extern.

După orice gate extern închis, sincronizează obligatoriu, în aceeași etapă:

1. `CURRENT_STATE.md`;
2. frontmatter-ul capitolului;
3. review-ul gate-ului.

Această regulă este critică: metadata stale a indus deja alte chat-uri în eroare.

Reader pass-ul nu poate redeschide sau slăbi doctrina. **În orice conflict, auditul doctrinar prevalează asupra celui stilistic.** Dacă o sugestie stilistică afectează doctrina, raportează conflictul și nu o aplica.

## Lecțiile cele mai importante din colaborarea cu auditorii

Nu verifica numai dacă textul conține erori; verifică și dacă lipsește o teză primară care schimbă rangul conceptului. Cele mai importante corecții recente au fost omisiuni într-un text altfel corect: `Erscheinungsebene` în cap. 35, lărgirea `Abwehrort` și `Wie ≠ Wieviel` în cap. 36, apoi forma serială, `0 -> ±`, conștient/inconștient și încărcările ThKP în cap. 37.

Semnele se verifică vizual. Cap. 36 a cerut controlul exact `+e / −hy / −k / +p`; cap. 37 a închis vechiul HOLD și a confirmat `Ø = Zwangsnullreaktion`.

Când auditorul extern corectează o evaluare preliminară din research, nu cosmetiza discrepanța: consemnează că auditul direct în corpus prevalează.

## Achiziții protejate imediat relevante

Cap. 31: complementaritate nu trebuie confundată cu integrare/Pontifex.  
Cap. 32: scara este `reacție + încărcare -> factor partener -> Vektorbild -> relația dintre vectori -> profil -> complement -> serie`.  
Cap. 33: `0` este plural și contextual; `Tendenzspannung ≠ Quantumspannung`; `!` nu este severitate clinică.  
Cap. 34: `±` factorial ≠ `Bitendenz` vectorială; `!` nu schimbă clasa structurală.  
Cap. 35: `korrelative Deutung`; `întregimea profilului ≠ totalitatea persoanei`; funcție ≠ conținut; corelație ≠ cauzalitate.  
Cap. 36: `Rand = S + C`, `Mitte = P + Sch`; metoda este `Wie`, nu `Wieviel`; `Mitte` nu se interpretează autonom; cenzurile exacte sunt `+e / −hy / −k / +p`; centrul poate purta `Affektgefahr / Ichgefahr`; `Abwehrort` general nu este limitat la Mitte.

## După recheck-ul CH37

Dacă auditorul acordă explicit `DOCTRINAL PASS`:

- sincronizează imediat capitolul + review-ul doctrinar + `CURRENT_STATE.md`;
- status: `DOCTRINAL PASS / SCIENTIFIC AUDIT CLOSED / STYLE FEEDBACK NEXT`;
- apoi reader pass stilistic;
- după integrarea reader pass-ului: `STABLE DRAFT` și creează `CH37_READER_PASS.md`;
- abia atunci deschide cap. 38.

Cap. 38 va fi **„De la profil la serie: de ce un singur profil nu este suficient”**. Nu începe direct cu calcule; mai întâi trebuie introdus timpul și schimbarea rangului afirmației.

## HOLD-uri la transfer

Active: `genotrop / genotropisch / genotropistisch`; sensurile tehnice ale `Schicksalsmöglichkeit`; diferențierile `Ich-Schicksal / Wahlschicksal / Freiheitsschicksal`; `Strebung -> năzuință` ca soluție lexicală de lucru.

Închis: vechiul HOLD `0` vs nul forțat — canonic, în EKP, **`Ø = Zwangsnullreaktion`** și nu se interpretează.

> **Cartea are prioritate asupra sistemului care ne ajută s-o scriem.**