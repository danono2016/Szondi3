# CH41 — Doctrinal review

**Capitol:** 41 — `TspG`, `TspD` și `Latenzproportionen`: de la factor la tensiunea vectorială  
**Status:** SCIENTIFIC HOLD / CH41-SHORT-01 SOURCE CONFLICT HOLD / DOCTRINAL RECHECK REQUIRED

## Baza de audit

- `research/CH41_RESEARCH.md` — RESEARCH COMPLETE / corpus pass 10/10 + bounded deep corpus pass + canonical visual formula check;
- `research/CH41_OUTLINE.md` — OUTLINE COMPLETE;
- `chapters/41_TspG_TspD_si_Latenzproportionen_de_la_factor_la_tensiunea_vectoriala_DRAFT.md` — DRAFT v2 / SCIENTIFIC HOLD / CH41-SHORT-01 SOURCE CONFLICT HOLD.

## Verdict extern integrat

**CAP.41 DRAFT v1 — DOCTRINALLY SOUND în nucleul 10+ profile, dar SCIENTIFIC HOLD pentru seria scurtă.**

Auditul extern a confirmat nucleul:

- `TspG = Σ(0 + ±)` pe factor, în serie;
- `TspG ≠ Quantumspannung` și `!` nu intră în sumă;
- `faktorieller TspG`, nu `Tages-TspG`, este baza pentru `TspD`;
- `TspD = TspG mai mare − TspG mai mic` intravectorial;
- indexul vectorului aparține factorului cu `TspG` mai mic, tratat de Szondi ca dinamic mai puternic;
- `TspD = 0` nu identifică un singur `Vektorbild`;
- `Latenzgrad / Latenzgröße` și `Latenzproportionen` sunt păstrate în rangul și terminologia sursei;
- egalitățile rămân egalități, fără tie-break inventat;
- Deri și Mélon rămân tradiție ulterioară;
- `Triebklasse/Unterklasse`, `Triebformel` și `TspQu` rămân în capitolele 42–44.

Auditul a cerut trei modificări, toate integrate în DRAFT v2:

1. transformarea `CH41-SHORT-01` din audit focus în **SOURCE CONFLICT HOLD**;
2. introducerea tezei primare din `Triebpathologie II` despre `Entladungsbereitschaft`, inclusiv calificarea `abgesehen von den Zwangsneurotikern`;
3. fix terminologic `täglicher TspG` -> **`Tages-TspG`**.

## CH41-SHORT-01 — SOURCE CONFLICT HOLD

Problema este o contradicție operațională internă în aceeași ediție a `Lehrbuch`-ului.

### Pasajul Schafir–Szondi

La pp. 285–286, instrucțiunile pentru seria scurtă cer `Umrechnung`, iar `Tabelle 13` convertește sumele reacțiilor în raport cu o `angenommene Zehnerserie`.

Acest pasaj poate sugera:

`sume factoriale -> Tabelle 13 -> TspG convertit -> TspD`.

### Contraexemplul canonic — Fall 18

În Fall 18, Szondi lucrează cu numai **6 profile** și tipărește:

`h=1, s=0, e=2, hy=2, k=5, p=4, d=3, m=3`.

Apoi calculează direct din aceste `TspG` brute:

`h−s = 1−0 = 1`  
`e−hy = 2−2 = 0`  
`k−p = 5−4 = 1`  
`d−m = 3−3 = 0`.

Rezultatul tipărit este deci:

`S=1, P=0, Sch=1, C=0`.

Dacă `TspG` ar fi convertite mai întâi prin `Tabelle 13`, pentru șase profile am avea:

`1→2`, `0→0`, `2→3`, `5→8`, `4→7`, `3→5`,

ceea ce ar produce:

`S=2, P=0, Sch=1, C=0`.

Dacă am converti direct `TspD` brute, ambele diferențe `1` ar deveni `2`, deci ar rezulta:

`S=2, Sch=2`.

Niciuna dintre variante nu reproduce Fall 18.

### Concluzia HOLD-ului

Sursa primară nu permite stabilirea neechivocă a unei reguli universale pentru seriile de 3–9 profile.

Manualul nu autorizează:

- nici `TspG brut -> Tabelle 13 -> TspD`;
- nici `TspD brut -> Tabelle 13`;

ca regulă canonică universală.

**Ambiguitatea documentată este preferabilă certitudinii inventate.**

HOLD-ul privește numai ordinea operațională a conversiei pentru seria scurtă. El nu invalidează mecanica direct documentată a `TspG`, `TspD` și `Latenzproportionen`.

## Adăugire conceptuală integrată

`Triebpathologie II` formulează explicit:

- `TspG` ca `Maßstab für die Entladungsbereitschaft eines Bedürfnisses`;
- `TspD` ca diferență a gradelor de `Entladungsbereitschaft` dintre cei doi factori ai vectorului și ca măsură cantitativă a tensiunii / `Triebgefahr` intravectoriale.

Explicația ambivalenței ca `Vorphase der Entladung` este calificată de Szondi prin excepția explicită:

**`abgesehen von den Zwangsneurotikern`.**

Formula de calcul `TspG = Σ0 + Σ±` rămâne neschimbată; excepția limitează universalizarea justificării doctrinare.

## Fix terminologic integrat

Eticheta canonică din tabel este:

**`Tages-TspG`**

și este păstrată distinct de:

**`faktorieller TspG`.**

## Gate

Auditul doctrinar **nu este închis**.

DRAFT v2 cere **doctrinal recheck** după integrarea celor trei modificări. `CH41-SHORT-01` rămâne SOURCE CONFLICT HOLD chiar dacă restul capitolului primește ulterior DOCTRINAL PASS, dacă nu apare o sursă canonică nouă care rezolvă contradicția.

Reader pass-ul este închis până la verdictul doctrinar explicit. Cap. 42 rămâne închis până la închiderea gate-ului doctrinar al cap. 41.