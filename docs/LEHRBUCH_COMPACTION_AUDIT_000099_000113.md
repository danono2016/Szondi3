# Lehrbuch P2A compaction audit — DR 000099–000113

**Source:** `SZ_LEHR_1972`

## Result

Original standalone objects: **15**  
Retained standalone cores: **5**  
Reduction: **10 objects / 66.7%**

## Retained cores

- `000099` — definition and physical/factor structure of the test: experimental Wahlverfahren; 6×8 = 48; each factor represented six times; factor result comes from sympathetic + unsympathetic choices.
- `000106` — critical anti-conflation rule: disease labels belong to photographed persons, not automatically to examinees choosing them.
- `000107` — `Nichts/Alles` / unusual low or high factor-choice frequency indicates current importance of the factor; detailed reaction meaning is deferred to later scoring doctrine.
- `000110` — Vordergänger/VGP and Hintergänger/EKP as complementary foreground/background fates, with explicit limit that they reveal only two Schicksalsmöglichkeiten and serial repetition is required.
- `000111` — essential administration rule: simultaneous display of each 8-card series and rapid relative choices yielding 2 sympathetic + 2 unsympathetic cards per series.

## Retirement map

- `000100` → `OMIT_NONCRITICAL` — provenance of patient photographs is historical/background, not needed as independent doctrine.
- `000101`, `000103`, `000105` → `MERGE_INTO_CORE -> 000099`.
- `000102` → `MERGE_INTO_CORE -> 000107`.
- `000104` → `OMIT_NONCRITICAL` — twin inference is historical support, not critical project doctrine.
- `000108`, `000109`, `000113` → `MERGE_INTO_CORE -> 000110`.
- `000112` → `MERGE_INTO_CORE -> 000111`.

## Fidelity safeguards

The exact 48-card / 6-series / 8-factor structure and sympathetic+unsympathetic scoring basis remain explicit. The warning against reading the photographed person's diagnostic label as the examinee's diagnosis remains independently retrievable. VGP/EKP complementarity is retained together with the serial-profile limitation, rather than as three redundant entries.
